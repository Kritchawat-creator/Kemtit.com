# Tracking Log

Record of implementation work on this project. See `CLAUDE.md` for the workflow rules this log follows.

Each entry:
- **Date**
- **Task**
- **Files changed**
- **Reason**
- **Result**

---

## 2026-09-05

- **Task**: Add a `.gitignore` file to the project.
- **Files changed**: `.gitignore` (new)
- **Reason**: Repo had no `.gitignore` yet; stack is Next.js/TypeScript with Supabase env vars, so build artifacts, `node_modules`, and secrets needed to be excluded before any real code lands.
- **Result**: Added standard Next.js/TypeScript ignores (`node_modules/`, `.next/`, `out/`, `.env*`, `*.tsbuildinfo`, etc.), plus `.gitnexus/` (local gitnexus index) and common editor/OS files (`.DS_Store`, `.vscode/`, `.idea/`).

## 2026-09-05

- **Task**: วิเคราะห์ `docs/kemtit-full-scope.md` และ `docs/kemtit-ui-design-system.md` แล้วเขียนแผนดำเนินการ POC (ยังไม่ implement — รอรีวิว)
- **Files changed**: `docs/implementation-plan.md` (new), `tracking-log.md`
- **Reason**: ต้องมีแผนที่เรียงตาม dependency จริง, รายการคำถาม/ข้อขัดแย้งระหว่าง 2 เอกสารให้เจ้าของโปรเจกต์ตัดสินใจ, และประมาณการเวลาที่สมจริงก่อนเริ่ม Milestone 1
- **Result**: แผนมี 3 ส่วน — (1) สรุปความเข้าใจ 10 บรรทัด, คำถาม 19 ข้อ (Q1-Q19) ที่ต้องตัดสินใจ, ความเสี่ยงเทคนิค 16 ข้อ (R1-R16); (2) M0 setup + M1-M6 milestones, ลำดับ component, จุดหยุดทดสอบ user 3 จุด (CP0-CP2), ประมาณ 7.5-9 สัปดาห์ (6-7 ถ้าใช้รายการตัด); (3) ส่วนที่ควรตัด/ประเมินต่ำไป/dependency ภายนอก + ข้อเสนอนอก scope + 3 สิ่งที่ต้องตัดสินใจก่อนเริ่ม — ยังไม่มีโค้ดใด ๆ ถูกสร้าง; รอการอนุมัติแผนก่อนเริ่ม M0/M1

## 2026-09-05 (รอบ 2)

- **Task**: รับ POC Decisions จากเจ้าของโปรเจกต์ (3 ข้อ: กฎ goal/schema, LINE+cron, รายการตัด) → ปรับ `docs/implementation-plan.md` เป็น v2 และแก้เอกสารต้นทางให้ตรง
- **Files changed**: `docs/implementation-plan.md` (เขียนใหม่ v2 + ภาคผนวก A บันทึก Decisions + ภาคผนวก B รายการไฟล์ M0), `docs/kemtit-full-scope.md` (§7 Flow A ข้อ 4-5 และ Flow C ข้อ 1-2, §12 แถว POC + footnote, §13 แถว POC/Dev), `docs/kemtit-ui-design-system.md` (§8.4 ติดป้าย POC/MVP), `tracking-log.md`
- **Reason**: Decisions มีลำดับความสำคัญสูงกว่า full-scope/design-system เมื่อขัดกัน จึงต้องทำให้ทั้ง 3 เอกสารเล่าเรื่องเดียวกันก่อนเริ่ม M0
- **Result**: POC = 2 widget fixed, `goal_kind` metric/execution, `task_completions`, LINE push เฉพาะ overdue (1/วัน) + goal สำเร็จ, GitHub Actions `*/5` + pg_cron fallback, ปฏิทิน วัน/สัปดาห์/เดือน; ตัด SalesVsGoal/ShopChecklist/DailyLife/digest/offline queue/WidgetPicker · ประมาณการใหม่ 7-9 สัปดาห์ (เป้า 6-7 ต้องใช้คันโยกตัดเพิ่มที่ระบุใน §2.7) · ยังไม่มีโค้ด — M0 รอยืนยันรายการไฟล์ในภาคผนวก B · จุดที่ยังเปิด: เวลาสแกน overdue 08:00, นิยาม streak รวม task เดี่ยว, ข้อความ template

## 2026-09-05 (รอบ 3 — Milestone 0)

- **Task**: M0 — scaffold โปรเจกต์ + tooling + design token + migration แรก ตามแผน v2 และ 7 ข้อแก้ไขของเจ้าของโปรเจกต์ (branch `feat/m0-scaffold`)
- **Files changed**:
  - scaffold/tooling: `package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml` (allowBuilds: supabase), `next.config.ts` (next-intl plugin), `tsconfig.json`, `postcss.config.mjs`, `eslint.config.mjs` (boundaries + hex + a11y), `.prettierrc`, `.prettierignore`, `.nvmrc` (22), `.env.example` (7 ตัวแปรตาม Decision + `NEXT_PUBLIC_LINE_OA_BASIC_ID`), `netlify.toml`, `components.json`, `vitest.config.mts`, `vitest.setup.ts`, `playwright.config.ts`, `.storybook/{main.ts,preview.tsx}`, `.github/workflows/ci.yml`
  - src: `src/styles/globals.css` (token ครบชุด + alias shadcn + ล้าง default ของ Tailwind), `src/styles/theme.ts`, `src/app/{layout.tsx,page.tsx,icon.svg}`, `src/i18n/request.ts`, `src/types/next-intl.d.ts`, `src/messages/th.json`, `src/lib/{utils.ts,utils.test.ts,env.ts,env.server.ts,env.test.ts}`, `src/components/ui/{button.tsx,button.stories.tsx}`, `public/.gitkeep`
  - supabase: `supabase/config.toml` (site_url, otp_expiry 600, template magic_link), `supabase/migrations/20260905133043_user_profiles.sql`, `supabase/templates/magic_link.html`, `supabase/.gitignore`
  - แก้ไข: `.gitignore`, `README.md` (ส่วน "เริ่มพัฒนา" รวมขั้นตอน Supabase 2 project / Resend SMTP / Netlify / LINE / GitHub), `AGENTS.md` (Next ใส่ block `nextjs-agent-rules` เองตอน dev — ใส่ล่วงหน้าให้ tree สะอาด; ส่วน gitnexus ไม่แตะ), `docs/implementation-plan.md` (sync ชื่อโฟลเดอร์ `shared-services/`, `NEXT_PUBLIC_APP_URL`, migration timestamp, กฎ lint)
- **Reason**: ตั้งฐานให้ M1 เริ่มเขียน feature ได้ทันทีโดยไม่ต้องย้อนแก้ tooling — ทำตาม 7 ข้อ: (1) migration ใช้ `supabase migration new` timestamp (2) trigger `handle_new_user` + RLS + column-level grant ในไฟล์เดียว (3) `lib/env.ts` validate แบบ lazy แยก client/server และข้ามเมื่อ `CI=true` (4) กฎ `no-restricted-imports` กัน modules import กันเอง และกัน `@supabase/*` ใน modules/shared-services/components (5) `.env.example` ครบทุกตัว (6) next-intl without routing (7) CI ไม่รัน Playwright
- **Result**: `pnpm lint` / `typecheck` / `test` (6 tests) / `build` (CI=true) / `build-storybook` ผ่านทั้งหมด; `next start` render หน้าไทย `lang="th"` ฟอนต์ Plex Thai theme-color ถูกต้อง · สิ่งที่ต่างจากรายการที่ยืนยัน: ใช้ `src/app/icon.svg` แทน `public/favicon.ico` (convention ของ Next), ใช้ package `cn` ของ shadcn แทน clsx+tailwind-merge เพราะ CLI v3 generate `import { cn } from "cn"`, ไม่ได้ติดตั้ง `eslint-plugin-boundaries` (ใช้ `no-restricted-imports` ตามที่ขอ), ใส่ block ของ Next ใน `AGENTS.md` · ทดสอบ migration บน Postgres 17 จริงใน Docker (จำลอง role anon/authenticated/service_role + `auth.users` + `auth.uid()`): apply ผ่าน, trigger สร้าง profile 2/2, RLS เห็นเฉพาะของตัวเอง, update แถวคนอื่นได้ 0 แถว, update `subscription_tier`/`line_user_id` จาก authenticated ถูกปฏิเสธ, anon อ่านไม่ได้, `updated_at` ขยับ, cascade delete ทำงาน — สคริปต์ทดสอบอยู่นอก repo (scratchpad) ยังไม่ได้เพิ่มเป็น `pnpm db:test` · GitHub Actions CI run แรกบน branch ผ่าน (lint/typecheck/test/build) · Storybook แจ้ง chunk > 500kB (เป็น runtime ของ Storybook เอง ไม่ใช่ของแอป) · button ของ shadcn สูง 36px ยังไม่ถึง 44px ตาม Design §10 — ปรับตอน M1 พร้อม primitives ที่เหลือ · รอเจ้าของโปรเจกต์: สร้าง Supabase 2 project + Resend SMTP + Netlify + LINE + GitHub secrets แล้ว `pnpm exec supabase link` + `pnpm db:push`

## 2026-09-05 (รอบ 4 — Milestone 1: Foundation, Auth OTP, Onboarding ขั้น 1-2) — branch `feat/poc`

- **Task**: M1 ตามแผน v2 — Supabase clients + proxy, auth email OTP, profile/persona, app shell, onboarding ขั้น 1-2, Supabase local สำหรับทดสอบ end-to-end
- **Files changed**: `src/lib/supabase/{server,client,admin}.ts`, `src/proxy.ts`, `src/lib/{date,format}.ts` (+ tests), `src/core/shared/result.ts`, `src/core/auth/{schema,actions}.ts`, `src/core/profile/{personas,schema,onboarding,queries,actions}.ts` (+ onboarding test), `src/modules/seller/persona.ts`, `src/app/(auth)/{layout.tsx,login/*,onboarding/persona/*}`, `src/app/(app)/{layout.tsx,dashboard,goals,calendar,settings}/page.tsx` (placeholder), `src/app/{layout,page}.tsx`, `src/components/layout/*` (AppShell, BottomNav, Sidebar, TopBar, UserMenu, QuickAddMenu, Fab, PageHeader, OnboardingSteps, QuickAddHost), `src/components/domain/EmptyState.tsx` (+ story), `src/components/ui/*` (shadcn primitives 20 ตัว, button ปรับ touch target ≥44px บนมือถือ, `form-i18n.tsx`), `src/messages/th.json`, `src/types/database.ts` (generate จาก local), `supabase/config.toml` (ปิด studio/realtime/storage/edge/analytics สำหรับ local, email_sent 100), `e2e/{helpers,onboarding.spec}.ts`, `playwright.config.ts`
- **Reason**: เจ้าของโปรเจกต์สั่ง "ทำให้จบ end to end" — ใช้ Supabase local (Docker) + Mailpit เพื่อทดสอบ OTP/RLS จริงโดยไม่ต้องรอบัญชี hosted; ข้อความ error จาก action เป็น key ใน `errors.*` ของ th.json (ห้าม hardcode ไทยใน core)
- **Result**: E2E `onboarding.spec.ts` ผ่านบน mobile-chrome: สมัครด้วย OTP จริง → เลือก seller → ถูกพาไป `/onboarding/first-goal` (สร้างใน M2) · lint/typecheck/23 unit tests ผ่าน · `.env.local` ชี้ local (ไม่ commit) · สิ่งที่รู้: shadcn `sonner` ดึง `next-themes` มาด้วย (ยังไม่ทำ ThemeProvider), `t()` แบบ dynamic key ต้อง cast เป็น union ของ key

## 2026-09-05 (รอบ 5 — Milestone 2: Goals, Cascade Engine, Progress, Onboarding ขั้น 3) — branch `feat/poc`

- **Task**: M2 ตามแผน v2 + POC Decisions 1.1/1.2/1.4 — schema goals/tasks/task_completions/domain_events, cascade engine, progress rollup, หน้าเป้าหมาย (list/detail), onboarding เป้าแรกจาก template ของ seller
- **Files changed**: `supabase/migrations/20260905154811_goals_tasks.sql` (4 ตาราง + RLS + trigger ตรวจ owner ของ parent/goal/task + index), `src/types/database.ts` (regen), `src/core/domain/{domains,periods,progress,recurrence,streak}.ts` (+ tests), `src/core/events/{types,emit}.ts`, `src/core/goals/{schema,queries,actions,candidates}.ts`, `src/core/tasks/schema.ts`, `src/core/profile/actions.ts` (+ `completeOnboarding`), `src/modules/seller/template.ts` (+ test), `src/components/domain/{DomainTag,DomainSelect,ProgressBar,ProgressRing,StatTile,PaceBadge,PeriodLabel,PeriodSwitcher,DatePicker,GoalCard,GoalCascadeTree,GoalForm,UpdateValueForm,Celebration}.tsx` (+ stories GoalCard/Progress/DomainTag), `src/components/ui/responsive-dialog.tsx`, `src/hooks/use-is-mobile.ts`, `src/components/layout/QuickAddHost.tsx` (เปิด GoalForm จาก `?new=goal[&parent=]`), `src/app/(auth)/onboarding/first-goal/{page,first-goal-form,actions,schema}.ts(x)`, `src/app/(app)/goals/{page.tsx,[id]/page.tsx,[id]/goal-detail-actions.tsx}`, `src/app/(app)/layout.tsx` (ส่ง parentCandidates), `src/lib/format.ts` (+ `formatThaiYear`, "บาท" = สกุลเงิน), `src/messages/th.json`, `e2e/{helpers,onboarding.spec,goals.spec}.ts`
- **Reason**: ทำตาม Decision — `goal_kind` metric/execution ห้ามผสม, progress เป็น pure function (`computeProgress` ตาม snippet + `buildProgressIndex` bottom-up), template = เป้าเดือน metric บาท + week execution ที่ทับเดือน + task ตัวอย่าง 1 ตัว/สัปดาห์; ช่วงลูกใช้กฎ overlap; ตาราง `domain_events` ย้ายมา M2 เพื่อ emit `goal.created`/`onboarding.completed` ได้ทันที
- **Result**: E2E ผ่านทั้ง 2 flow บน mobile-chrome (onboarding เต็ม → เห็นเป้าเดือน + 5 week goal → อัปเดตยอด 25,000/50,000 → 50%; สร้างเป้าสุขภาพผ่านปุ่มเพิ่ม → เห็นใน filter ชีวิตส่วนตัว) · unit 43 tests · build ผ่าน · บั๊กที่เจอและแก้: ไฟล์ `"use server"` ห้าม export Zod schema (กลายเป็น server reference), client component ห้าม import จากไฟล์ `server-only` (แยก `candidates.ts`), `FormControl` ต้องครอบ input ตรง ๆ ไม่ใช่ div · ยังไม่ทำ: task ซ้ำใน execution progress (ตัดสินไม่นับ — บันทึกใน progress.ts), seed.sql (E2E สร้างข้อมูลเองผ่าน UI)

## 2026-09-05 (รอบ 6 — Milestone 3: Tasks, Recurring, Completion History, Undo) — branch `feat/poc`

- **Task**: M3 ตามแผน v2 + Decision 1.3 — task CRUD, recurring subset, `task_completions`, ติ๊ก/ยกเลิก, เลื่อนวัน, ลบแบบ undo, goal.completed สำหรับ execution goal
- **Files changed**: `src/core/domain/dayplan.ts` (+ test: ประกอบงานของวัน/ค้าง/เสร็จ และงานของ goal), `src/core/tasks/{schema,queries,actions}.ts`, `src/components/domain/{TaskRow,TaskList,TaskForm}.tsx` (+ TaskRow stories), `src/components/layout/QuickAddHost.tsx` (`?new=task[&goal=&date=]`), `src/app/(app)/goals/[id]/page.tsx` (ใช้ TaskList), `src/messages/th.json` (tasks.*), `e2e/tasks.spec.ts`
- **Reason**: task ซ้ำใช้ `task_completions` ต่อวัน (BKK) ส่วน task เดี่ยวใช้ `completed_at`; occurrence ของ task ซ้ำที่ผ่านไปไม่ถือว่า "ค้าง"; ติ๊กแล้วเช็ค execution goal และแม่ขึ้นไปถึง 100% ครั้งแรก → `completed_at` + `goal.completed` (Design §8.5: undo toast 5 วิ แทน confirm)
- **Result**: E2E 4 flow ผ่านบน mobile-chrome (เพิ่มงานให้ week goal → ติ๊กครบ → toast "ทำได้แล้ว" + 100%; task ซ้ำทุกวัน ติ๊ก/ยกเลิก คงอยู่หลัง reload) · unit 46 · บั๊กที่เจอ: E2E ต้องรอ response ของ server action ก่อน reload (optimistic UI) และ radio ของ Radix ที่ sr-only ต้องคลิกผ่าน label · ยังไม่ทำ: story ของ TaskList (import server action → `server-only` พังใน Storybook — ใช้ TaskRow story แทน), swipe/long-press (Q6 ใช้ Sheet)

## 2026-09-05 (รอบ 7 — Milestone 4: Dashboard คงที่ 2 widget) — branch `feat/poc`

- **Task**: M4 ตาม Decision 3 — dashboard layout คงที่: `GoalProgressWidget` (เป้าหลักเดือนนี้) บนสุด + `TodayTasksWidget` (งานวันนี้ + streak), widget registry ที่ app layer, skeleton, empty state ภายใน widget
- **Files changed**: `src/components/widgets/{WidgetShell,WidgetSkeleton,GoalProgressWidget,GoalProgressPanel,TodayTasksWidget}.tsx` (+ stories WidgetShell/GoalProgressPanel), `src/app/(app)/dashboard/{registry.ts,page.tsx}`, `src/core/tasks/queries.ts` (+ `getStreak`), `src/components/domain/TaskList.stories.tsx`, `.storybook/{main.ts,preview.tsx,mocks/*}` (alias mock `server-only`/`next/cache`/`next/headers`/supabase server + NextIntlClientProvider decorator), `src/messages/th.json` (widgets.*), `e2e/dashboard.spec.ts`
- **Reason**: Design §8.4 — widget แรกบนสุดคือ goal หลักเดือนนี้ (metric ก่อน execution), empty state ภายใน widget พร้อม CTA; registry แยกจาก core เพื่อให้ MVP เติม react-grid-layout/dashboard_layouts จุดเดียว; streak แสดงเพื่อให้ metric §14 ข้อ 2 มองเห็นได้จริง
- **Result**: E2E 5 flow ผ่าน (dashboard: 0% → อัปเดตยอด 50% จาก widget, เพิ่มงานจาก widget แล้วติ๊ก → "ทำต่อเนื่อง 1 วัน") · Storybook build ผ่านรวม story ของ TaskList/GoalProgressPanel ที่ import server action (ผ่าน alias mock) · lint 0 error · ยังไม่วัด bundle ตาม R8 (ไม่มี Recharts แล้ว จะวัดด้วย Lighthouse ใน M7)

## 2026-09-05 (รอบ 8 — Milestone 5: Domain events, Cron, LINE) — branch `feat/poc`

- **Task**: M5 ตาม Decision 2 — ผูก LINE ด้วยรหัส 6 ตัวผ่าน webhook, processor ของ `domain_events`, job สแกนงานเลยกำหนด (1 event/user/วัน), push เฉพาะ overdue + goal สำเร็จ, GitHub Actions cron, หน้าตั้งค่า
- **Files changed**: `src/core/ports/{notifier,ai-suggestion}.ts`, `src/core/profile/{line,line.test,admin,line-actions}.ts`, `src/core/tasks/admin.ts`, `src/core/events/admin.ts`, `src/shared-services/notifications/line/{signature,url,client,notifier,messages}.ts` (+ tests), `src/shared-services/events/{processor,run}.ts` (+ test 5 กรณี), `src/shared-services/jobs/{scan-overdue,run}.ts` (+ test), `src/lib/http/cron-auth.ts`, `src/lib/env.server.ts` (LINE token ว่าง = dry-run), `src/app/api/{cron/process-events,cron/scan-overdue,line/webhook}/route.ts`, `src/app/(app)/settings/{page,display-name-form,line-link-card,notify-switch}.tsx`, `.github/workflows/{cron-events,cron-scan-overdue,keepalive}.yml`, `src/messages/th.json` (settings.*, line.*), `.env.example`, `e2e/line.spec.ts`
- **Reason**: shared-services ห้ามแตะ Supabase ตรง (Decision M0 ข้อ 4) → core เปิด admin repo (`core/{events,profile,tasks}/admin.ts`) และ port `Notifier`; processor/scan job ฉีด dependencies เพื่อ unit test ได้; ไม่มี LINE OA จริง → `DryRunNotifier` log แทนส่งและบันทึก `notification.sent dryRun=true` สลับเป็นของจริงเมื่อใส่ `LINE_CHANNEL_ACCESS_TOKEN`; ทุกลิงก์ใน LINE ต่อ `openExternalBrowser=1` (R3); cron ต้องมี bearer (R7)
- **Result**: E2E 6 flow ผ่าน — flow LINE: ขอรหัสในตั้งค่า → webhook ที่ signature ผิดได้ 401 / ถูกได้ 200 และหน้าตั้งค่าเปลี่ยนเป็น "เชื่อมแล้ว" → สร้าง task ค้างเมื่อวาน → `/api/cron/scan-overdue` สร้าง event → `/api/cron/process-events` ส่ง (dry-run) → ยกเลิกการเชื่อม · unit 58 · ยังไม่ทดสอบกับ LINE จริง (ต้องมี channel ของเจ้าของโปรเจกต์) · workflow cron ข้ามตัวเองถ้าไม่มี secret `CRON_BASE_URL`/`CRON_SECRET` · ไม่มี migration ใหม่ (คอลัมน์ line_*/notify มีตั้งแต่ M0, domain_events ตั้งแต่ M2)

## 2026-09-05 (รอบ 9 — Milestone 6: ปฏิทิน วัน/สัปดาห์/เดือน) — branch `feat/poc`

- **Task**: M6 ตาม Decision 3 — ปฏิทิน 3 มุมมอง (ตัดมุมมองปี) สัปดาห์เริ่มอาทิตย์ task เป็นจุดสี domain + ชื่อ แตะวัน → มุมมองวัน (TaskList เดียวกับแดชบอร์ด)
- **Files changed**: `src/core/domain/calendar.ts` (+ test), `src/components/domain/{CalendarNav,CalendarWeek,CalendarMonth}.tsx` (+ Calendar stories 4 state), `src/app/(app)/calendar/page.tsx`, `src/messages/th.json` (calendar.*), `e2e/calendar.spec.ts`
- **Reason**: ใช้ลิงก์ `?view=&date=` แทน state ฝั่ง client เพื่อให้ SSR/back-forward ทำงานและไม่ต้อง fetch ซ้ำ; เดือนดึงช่วง grid (อาทิตย์แรก–เสาร์สุดท้าย) ครั้งเดียวแล้วกระจาย occurrence ของ task ซ้ำด้วย pure function
- **Result**: E2E ปฏิทินผ่าน (งานที่เพิ่มปรากฏใน สัปดาห์/เดือน/วัน, เลื่อนสัปดาห์และกลับวันนี้) · unit 63 · มือถือ: สัปดาห์เรียงเป็นวัน เดือนเป็น grid จุดสี

## 2026-09-05 (รอบ 10 — Milestone 7: PWA, state ครบ, DoD sweep, metrics, เอกสาร) — branch `feat/poc`

- **Task**: M7 — PWA (manifest, icon, service worker, offline banner อ่านอย่างเดียว, คำแนะนำติดตั้ง), loading/error/not-found, story ที่ขาดตาม DoD, metrics SQL ของ §14, Lighthouse, เอกสาร
- **Files changed**: `src/app/{manifest.ts,sw.ts,not-found.tsx}`, `src/app/(app)/{loading,error}.tsx`, `src/components/layout/{OfflineBanner,InstallHint}.tsx`, `scripts/generate-icons.mjs` → `public/icons/*.png`, `next.config.ts` (Serwist, ปิดใน dev), `tsconfig.json` (webworker + serwist typings), `package.json` (`build` = `next build --webpack`), `.gitignore` (sw output), `src/styles/theme.ts` (+ `pageBackgroundColor`), `src/styles/globals.css` (`--color-text-muted` → neutral-600), `src/app/layout.tsx` (manifest/appleWebApp/icons), `src/app/(app)/dashboard/page.tsx` (InstallHint), `src/app/(app)/goals/[id]/page.tsx` (formatNumber), `src/components/domain/{Inputs,Forms,GoalCascadeTree}.stories.tsx`, `supabase/queries/poc-metrics.sql`, `src/messages/th.json` (pwa.*, errors.*), `README.md`, `docs/implementation-plan.md` (สถานะการทำงาน)
- **Reason**: R5 ยืนยันจริง — `@serwist/next` ไม่ทำงานกับ Turbopack (build เตือนและไม่สร้าง sw.js) จึงให้ production build ใช้ webpack ส่วน dev ยังใช้ Turbopack (SW ปิดใน dev อยู่แล้ว); icon สร้างด้วยสคริปต์ PNG encoder เล็ก ๆ จากสี token ไม่พึ่ง lib ภาพ; Lighthouse พบ `text-text-muted` (neutral-400) contrast 2.2:1 ไม่ผ่าน AA ทั้งที่ Design กำหนด token นี้ → ปรับ mapping เป็น neutral-600 และสงวน neutral-400 ให้ icon/เส้น
- **Result**: E2E 7 spec ผ่านครบ · Storybook build ผ่านทุก story · `pnpm build` (webpack) สร้าง `public/sw.js` 52 KB · Lighthouse mobile จำลอง 4G หน้า /login: performance 89 / a11y 96 (100 หลังแก้ contrast) / best-practices 96, FCP 0.8s, CLS 0, TBT 50ms, **LCP 3.8s ยังเกิน budget 2.5s** และ **JS โอน ~335 KB เกิน budget 200 KB** (baseline React+Next + RHF/zod/Radix ของหน้า login + messages ทั้งไฟล์) — บันทึกเป็นงาน MVP: แยก messages ต่อ namespace, ลด zod/RHF บน client ของหน้า login, วัดหน้า dashboard หลัง login ด้วย · ยังไม่ทดสอบ: LINE จริง, Netlify deploy จริง, iOS install จริง (ต้องอุปกรณ์/บัญชีของเจ้าของ)

## 2026-09-06 (รอบ 11 — แก้ตามรีวิว 4 ข้อของเจ้าของโปรเจกต์) — branch `feat/poc`

- **Task**: (1) ส่ง LINE ซ้ำได้ถ้า function ตายหลัง push → บันทึกเป็น known issue (แก้ใน MVP ตามที่กำหนด) (2) `CI=true` ข้าม env validation → ข้ามเฉพาะ `SKIP_ENV_VALIDATION=1` (3) scan-overdue 25 คน/รอบ → known issue + แนวทาง cron รายชั่วโมงใน MVP (4) ฟอนต์โหลดจาก Google ตอน build → `next/font/local`
- **Files changed**: `src/lib/env.ts`, `src/lib/env.test.ts`, `.github/workflows/ci.yml` (`SKIP_ENV_VALIDATION: "1"`), `src/styles/theme.ts` (localFont), `src/styles/fonts/{IBMPlexSansThai-400,500,600}.woff2` + `LICENSE.txt` (OFL, subset thai+latin จาก google-webfonts-helper), `src/shared-services/events/processor.ts` (comment known issue), `eslint.config.mjs` + `.prettierignore` (ignore `public/sw.js` ที่ build สร้าง), `docs/implementation-plan.md` (Known issues), `README.md`
- **Reason**: Netlify ตั้ง `CI=true` เองและอาจหลุดไป runtime → secret ที่ขาดจะ crash แบบอ่านไม่ออก; build ที่พึ่ง Google Fonts ล้มเมื่อเครือข่ายถูกบล็อก (เกิดจริงในเครื่องของเจ้าของโปรเจกต์) และถ่วง LCP
- **Result**: unit 63 (test env ใหม่: `CI=true` อย่างเดียวต้อง throw พร้อมชื่อตัวแปร) · build webpack self-host woff2 3 ไฟล์ ไม่มี request ไป fonts.gstatic · Lighthouse /login mobile: performance 89→94, accessibility 96→100 (contrast แก้แล้ว), LCP 3.8→3.0s, CLS 0, TBT 60ms — LCP ยังเกิน 2.5s (เหลือเรื่อง client bundle) · ข้อ 1 และ 3 ไม่แก้โค้ดตามที่กำหนดให้ทำใน MVP

## 2026-09-06 (รอบ 12 — QA บน localhost ตามคำขอ) — branch `feat/poc`

- **Task**: QA ระบบบน localhost ทั้งมือถือและ desktop (เบราว์เซอร์ในแอปคลิกค้าง จึงใช้ Playwright แบบสคริปต์ `e2e/qa/localhost-qa.spec.ts` ถ่าย screenshot ทุกขั้น + เก็บ console error) แล้วแก้สิ่งที่พบ
- **Files changed**: `e2e/qa/localhost-qa.spec.ts` (ใหม่ — QA 25 ขั้นมือถือ + 6 ขั้น desktop, ไม่รันใน CI), `.claude/launch.json` (dev / dev-attached / storybook), `src/app/(auth)/login/login-form.tsx` (cooldown 60 วิ + อีเมลแยกบรรทัดในหัวข้อ OTP), `src/components/layout/QuickAddHost.tsx` (เป้าย่อยใหม่ default เป็นช่วงปัจจุบัน), `src/app/(app)/goals/[id]/page.tsx` (tile "สิ้นสุด" + "ครบเป้าแล้ว"), `src/components/layout/Fab.tsx` (ซ่อนบนหน้าตั้งค่า), `src/app/layout.tsx` (toast ไม่ทับ top bar), `src/app/(app)/calendar/page.tsx` (empty state ตามวันที่เลือก), `e2e/calendar.spec.ts` (แก้ race), `src/messages/th.json`, `.gitignore` (qa-screenshots/)
- **Reason / สิ่งที่พบ**: (1) ส่งรหัสซ้ำไปอีเมลเดิมภายใน ~60 วิได้ 429 จาก Supabase แต่ปุ่ม resend ของแอปเปิดที่ 30 วิ → ปรับเป็น 60 วิ (2) "เพิ่มเป้าย่อย" prefill เป็นสัปดาห์แรกของเดือนที่ผ่านไปแล้ว → ใช้สัปดาห์ปัจจุบัน (3) tile ของ execution goal ป้าย "เหลือ N วัน" แต่ค่าเป็นวันที่ → แยกเป็น "สิ้นสุด" + hint; metric ที่ครบเป้าแสดง "ครบเป้าแล้ว" แทน "—" (4) FAB ทับสวิตช์ในหน้าตั้งค่า → ซ่อน (5) toast ทับ top bar บนมือถือ → offset 64px (6) หัวข้อ OTP ตัดอีเมลยาวข้าม 3 บรรทัด → แยกบรรทัด (7) empty state ของมุมมองวันเขียน "วันนี้" ทั้งที่เลือกวันอื่น (ทำให้ E2E ปฏิทิน flaky ด้วย) → ข้อความตามวันที่ · ไม่พบ console error/warning; hydration warning ที่เห็นครั้งแรกมาจาก caret-color ที่ Playwright ใส่ตอนถ่ายภาพ ไม่ใช่แอป
- **Result**: QA มือถือ 25/25 · desktop 6/6 · regression E2E 7/7 · unit 63 · lint 0 error · หมายเหตุ: หน้า not-found ตอบ HTTP 200 (streaming ผ่าน loading.tsx ตามพฤติกรรม Next — UI ถูกต้อง) · ยังไม่ทดสอบ: ออฟไลน์จริง, ติดตั้ง PWA บนเครื่องจริง, LINE จริง
## 2026-09-06 (รอบ 13 — ปรับ UI/UX มือถือตาม Claude Design "Kemtit" โทน 2a) — branch `feat/poc`
- **Task**: นำดีไซน์จากโปรเจกต์ Claude Design (`Kemtit.dc.html` + `support.js`) มาใช้กับแอปทั้งชุดมือถือ: Compass Dial, dashboard "ทิศทางวันนี้", เป้าหมาย/รายละเอียด (เส้นทาง waypoint), bottom sheet สร้างงาน, ปฏิทินสัปดาห์แบบแถบวัน, ตั้งค่า, login/OTP/persona/ตั้งเป้าแรก, empty/loading/offline, toast โทนสว่าง — ใช้ token เดิมทั้งหมด ไม่เพิ่มสีใหม่
- **Files changed**: `src/styles/globals.css` (bg-page = brand-50, radius xl 24/2xl 28, เงา sm/md/lg ใหม่ + shadow-nav/sheet/toast/fab, drop-shadow-dial, keyframes km-pulse), `src/components/ui/{button,input,textarea,switch,skeleton,sonner,input-otp,select,dropdown-menu,dialog,sheet,label,checkbox,radio-group,toggle,responsive-dialog}.tsx`, `src/components/ui/segmented-nav.tsx` (ใหม่ — ใช้ร่วมกันหน้าเป้าหมาย/ปฏิทิน), `src/components/domain/CompassDial.tsx` (ใหม่ — SVG หน้าปัด/เข็ม/sweep/pulse ใช้ class token), `src/components/domain/{ProgressBar,PaceBadge,DomainTag,EmptyState,GoalCard,GoalCascadeTree,TaskRow,TaskList,DomainSelect,PeriodSwitcher,CalendarNav,CalendarWeek,CalendarMonth,StatTile}.tsx`, `src/components/layout/{AppShell,TopBar,UserMenu,BottomNav,Fab,QuickAddMenu,PageHeader,OfflineBanner,InstallHint,OnboardingSteps}.tsx`, `src/components/widgets/{WidgetShell,WidgetSkeleton,GoalProgressWidget,GoalProgressPanel,TodayTasksWidget}.tsx`, `src/app/(app)/{dashboard,goals,goals/[id],calendar,settings}/page.tsx`, `src/app/(app)/settings/{display-name-form,line-link-card,notify-switch}.tsx`, `src/app/(app)/{loading,error}.tsx`, `src/app/not-found.tsx`, `src/app/(auth)/layout.tsx`, `src/app/(auth)/login/login-form.tsx`, `src/app/(auth)/onboarding/{persona/page,persona/persona-picker,first-goal/page,first-goal/first-goal-form}.tsx`, `src/lib/format.ts` (+ `formatValueParts`, `formatWeekdayNarrow`), `src/lib/date.ts` (+ `hourBkk`), `src/messages/th.json` (dashboard.heading/greeting ตามช่วงเวลา, pace ภาษาเข็มทิศ, goals.route/youAreHere/destination, widgets.*, onboarding.stepShort/perWeekHint, pwa.offline), stories (Progress/Dial, EmptyState, GoalProgressPanel, WidgetShell, Calendar), `e2e/qa/localhost-qa.spec.ts` (ข้อความใหม่ + แก้ race มุมมองเดือน + เลือกวันจันทร์/พุธในสัปดาห์), `.claude/launch.json` (prettier)
- **Reason / วิธีนำเข้าดีไซน์**: `DesignSync` (claude_design MCP) ต้อง `/design-login` จาก session แบบโต้ตอบซึ่งรันที่นี่ไม่ได้ → อ่านไฟล์ผ่านหน้าโปรเจกต์ใน Chrome ของเจ้าของ (API `GetFile` ของ claude.ai/design) แล้วดึงเนื้อหาผ่าน page text; `support.js` เป็น runtime ของ .dc.html เท่านั้น ไม่ต้องพอร์ต · การตัดสินใจระหว่างทาง: (1) เก็บ aria-label/ข้อความที่ E2E ใช้ (region "เป้าหลักเดือนนี้", ปุ่ม "อัปเดตยอด", ลิงก์ "เพิ่มงาน") แม้หน้าตาเปลี่ยน — % ของหน้าปัดเป็น HTML ซ้อน SVG เพื่อให้อ่าน/ทดสอบได้ (2) ป้ายสถานะเปลี่ยนเป็นภาษาเข็มทิศ ตามเส้นทาง/ออกนอกเส้นทาง/ถึงจุดหมายแล้ว; หัวข้อเป้าย่อยเป็น "เส้นทาง" (waypoint: ปลายทางบนสุด → สัปดาห์ล่าสุดลงมา, "คุณอยู่ตรงนี้" ที่ช่วงปัจจุบัน) (3) มุมมองสัปดาห์บนมือถือ = แถบ 7 วัน + งานของวันที่เลือก (`?date=`) ส่วน desktop ยังเป็น 7 คอลัมน์ — สลับด้วย `useIsMobile` เพื่อไม่ให้ชื่องานซ้ำใน DOM (4) ปุ่ม "อัปเดตยอด/ดูเป้าหมาย" ยังอยู่ใน hero แม้ดีไซน์ไม่มี เพราะเป็น flow หลักของ POC (5) ชื่อวันในปฏิทินใช้ `weekday: "narrow"` (อา จ อ พ พฤ ศ ส) เพราะ ICU ไทย "short" ให้ชื่อเต็ม (6) ปุ่มย้อนกลับหน้า OTP ใช้ป้าย "ย้อนกลับ" ไม่ซ้ำกับปุ่ม "เปลี่ยนอีเมล" (QA strict mode) (7) FAB/pill persona/ปลายเข็ม ใช้ accent 3 จุดตามที่ดีไซน์ 2a ตกลงไว้
- **Result**: typecheck ผ่าน · lint 0 error (warning form.watch เดิม 3 จุด) · unit 63 · Storybook build ผ่าน · E2E mobile-chrome 8/8 · QA localhost มือถือ 25/25 + desktop 6/6 · ตรวจด้วยตาบน 375×812: dashboard (0% / 63% / 100% + pulse), เป้าหมาย, รายละเอียด + เส้นทาง, sheet สร้างงาน, ปฏิทินสัปดาห์/เดือน, ตั้งค่า, login/OTP/persona/ตั้งเป้าแรก, toast สว่าง; desktop 1440×900 ไม่พัง (sidebar + hero 2 คอลัมน์ + ปฏิทิน 7 คอลัมน์) · ยังไม่ทำ: desktop 4 หน้าตามคำขอรอบถัดไปในโปรเจกต์ดีไซน์ (ยังไม่มีในไฟล์ .dc.html — งานถูกขัดจังหวะ), ปฏิทินเดือนของดีไซน์ (ใช้ grid เดิมแต่งใหม่), Lighthouse หลังปรับ (การ์ดมีเงาเพิ่ม ควรวัด LCP อีกครั้งใน MVP)
## 2026-09-06 (รอบ 14 — Desktop layout + รูปภาพ ตาม Claude Design turn 4/5) — branch `feat/poc`
- **Task**: นำดีไซน์ที่เพิ่มในโปรเจกต์ Claude Design มาใช้ต่อ: (turn 4) desktop 1440×900 — sidebar ซ้ายพับได้, header 72px + toolbar grid 12 คอลัมน์, dashboard ยอดขาย 8/งานวันนี้ 4 พร้อม tile waypoint, เป้าหมาย 3 คอลัมน์, detail hero 4/เนื้อหา 8, ปฏิทินเดือน 8 + แผงงานของวันที่เลือก 4 · (turn 5) จุดแสดงรูปทั้ง mobile/desktop — ภาพเป้าหมาย (cover), gallery รูปความคืบหน้า, รูปแนบงาน (thumbnail ใน TaskRow + โซนแนบในฟอร์ม/รายละเอียดงาน), รูปโปรไฟล์ — ทุกช่องอัปโหลดจริง ลากวาง/แตะ รูปค้างอยู่หลังรีโหลด
- **Files changed**: `supabase/migrations/20260906120000_photos.sql` (ใหม่ — `user_profiles.avatar_path` + grant รายคอลัมน์, `goals.cover_path`, ตาราง `goal_photos`/`task_photos` + RLS + trigger owner, bucket `photos` public-read + policy โฟลเดอร์ `<uid>/`), `supabase/config.toml` (`[storage] enabled = true`), `src/types/database.ts` (regen), `src/lib/supabase/storage.ts` (ใหม่ — bucket/limit/URL helper), `src/core/photos/{schema,actions,queries}.ts` (ใหม่ — `attachPhoto`/`removePhoto` + เพดาน 12 รูป/เป้า 5 รูป/งาน), `src/core/tasks/{schema,queries}.ts` (embed `photos:task_photos`), `src/core/goals/queries.ts` (`GoalDetail.photos`), `src/hooks/use-photo-upload.ts` (ใหม่ — ย่อรูป ≤1600px → อัปโหลดตรง Storage → attach → refresh), `src/components/domain/{ImageSlot,PhotoViewer,GoalPhotos,TaskPhotos}.tsx` (ใหม่), `src/components/domain/{TaskRow,TaskList,TaskForm,CompassDial,CalendarNav,CalendarMonth}.tsx`, `src/components/layout/{ShellFrame(ใหม่),Sidebar,TopBar,AppShell,PageHeader,UserMenu}.tsx`, `src/components/widgets/{GoalProgressWidget,GoalProgressPanel}.tsx`, `src/app/(app)/{dashboard/page,dashboard/registry,goals/page,goals/[id]/page,calendar/page,settings/page}.tsx`, `src/styles/globals.css` (`--shadow-sidebar`), `src/lib/format.ts` (`longWeekday`), `src/messages/th.json` (photos.*, errors.photo*, nav.collapse/expand, goals.aheadOfPlan…, calendar.todayLabel), stories (GoalCard/GoalCascadeTree/GoalProgressPanel fixture `cover_path`, Calendar nav), `e2e/photos.spec.ts` (ใหม่ — 4 flow อัปโหลด + ลบ), `e2e/qa/localhost-qa.spec.ts` (desktop: ไม่มีเมนู "เพิ่ม" กลางแล้ว → ใช้ปุ่มใน toolbar + ทดสอบพับ sidebar + แผงวันในปฏิทินเดือน; mobile: locator `เป้า ฿40,000`), `README.md`
- **Reason / การตัดสินใจ**: (1) client อัปโหลดตรงเข้า Storage (RLS `(storage.foldername(name))[1] = auth.uid()`) แล้วค่อยเรียก server action ผูก path — เลี่ยง body limit 1 MB ของ server action และไม่ต้องส่งไฟล์ผ่าน Next (2) `user_profiles` ให้สิทธิ์ update รายคอลัมน์อยู่แล้ว → ต้อง `grant update (avatar_path)` ไม่งั้น attach avatar ล้มแบบ generic (เจอตอนทดสอบจริง) (3) `[storage]` ใน config.toml เดิมปิดอยู่ → migration ที่แตะ `storage.buckets` ล้ม จึงเปิดและ restart stack (ข้อมูล local ไม่หาย) (4) desktop: TopBar ลอย absolute มุมขวาของ header 72px, toolbar อยู่ใน PageHeader (`toolbarStart` ทุกจอ / `toolbarEnd` desktop) เพื่อไม่ต้อง render ตัวควบคุมซ้ำ; tile waypoint บน hero render เฉพาะ desktop ด้วย `useIsMobile` (ถ้าซ่อนด้วย CSS ตัวเลข "40,000" จะซ้ำใน DOM และ Playwright strict mode ล้ม) (5) ปุ่ม "เพิ่ม" กลางใน top bar desktop ถูกถอดตามดีไซน์ (ปุ่มหลักย้ายไป toolbar ของแต่ละหน้า) (6) ปฏิทินเดือน desktop คลิกวัน → คงมุมมองเดือน (`?date=`) + แผงงานขวา; มือถือยังไปมุมมองวัน — `CalendarMonth` เป็น client component สลับตาม viewport (7) รูปในฟอร์มสร้างงานเลือกไว้ก่อน (พรีวิวจาก object URL) แล้วอัปโหลดหลังบันทึกงานสำเร็จ เพราะยังไม่มี task id · การตรวจด้วยมือในเบราว์เซอร์ในแอปเชื่อไม่ได้ (pane ซ่อน → hydration ค้าง) จึงยืนยันด้วย `e2e/photos.spec.ts` แทน
- **Result**: typecheck ผ่าน · lint 0 error (warning form.watch เดิม 3 จุด) · unit 63 · Storybook build ผ่าน · E2E mobile+desktop 16/16 (7 spec × 2) + `photos.spec` ผ่านทั้ง 2 project · QA localhost มือถือ 25/25 · desktop 7/7 (เพิ่มขั้นพับ sidebar + แผงวันปฏิทินเดือน) · ตรวจด้วยตา desktop 1440×900: dashboard/เป้าหมาย/detail (อัปโหลด cover เห็นรูปจริง)/ปฏิทินเดือน+แผงวัน; มือถือ: detail + cover, ฟอร์มงานมีโซนแนบรูป, ตั้งค่ามี avatar เป็นรูป · ยังไม่ทำ: desktop หน้าตั้งค่า/สร้างงานแบบ modal (ดีไซน์ยังไม่มี), lightbox ปัดดูรูปถัดไป, จำกัดจำนวนรูปตามแพ็กเกจ (ดีไซน์ระบุเป็นงานถัดไป), ลบไฟล์ใน bucket เมื่อ goal/task ถูกลบ (แถวลบตาม cascade แต่ไฟล์ค้าง — ควรมี cleanup job ใน MVP), Lighthouse หลังเพิ่มรูป

## 2026-09-06 (รอบ 15 — เพิ่มกฎ AI Model Routing) — branch `readme-content`

- **Task**: เพิ่มกฎ "AI Model Routing" ลงใน `CLAUDE.md` และ `AGENTS.md` — กำหนด model ที่ใช้ตาม role ของงาน (คิด/วางแผน, เขียนโค้ด, advisor) แยกตาม coding agent system ที่รัน (Claude system / GPT system / Antigravity)
- **Files changed**: `CLAUDE.md` (เพิ่ม section ใหม่ท้ายไฟล์), `AGENTS.md` (เพิ่ม section เดียวกันต่อจาก `<!-- END:nextjs-agent-rules -->`, ไม่แตะ block `gitnexus:start/end` หรือ `BEGIN/END:nextjs-agent-rules` ที่ tool อื่น generate เอง)
- **Reason**: เจ้าของโปรเจกต์ต้องการ routing policy ให้แต่ละ system เลือก model ตาม role ที่เหมาะสม (planning ใช้รุ่นคิดหนัก, implementation ใช้รุ่นเขียนโค้ดที่ effort สูงสุด, advisor ใช้รุ่นเดียวกับ planning) — บันทึกไว้ในทั้ง 2 ไฟล์เพราะบาง tool อ่าน `CLAUDE.md` บางตัวอ่าน `AGENTS.md`
- **Result**: ทั้ง 2 ไฟล์มี section "## AI Model Routing" เนื้อหาตรงกัน ระบุ 3 systems (Claude / GPT / Antigravity) แต่ละอันมี 3 ข้อ (planning model, implementation model + effort, advisor model) — ไม่มีการแก้โค้ด ไม่มีการรัน test (เอกสารล้วน)

## 2026-09-06 (รอบ 16 — วิเคราะห์และเพิ่ม .gitignore) — branch `feat/poc`

- **Task**: วิเคราะห์ `.gitignore` ปัจจุบันเทียบกับ `git status --ignored`, `package.json` (Next 16/pnpm/Playwright/Vitest/Storybook/Serwist/Supabase CLI), `playwright.config.ts`, `vitest.config.mts`, `supabase/.gitignore` (nested), และ `git ls-files` (หา secret/artifact ที่หลุดเข้าไปอยู่แล้ว) แล้วเพิ่มเฉพาะช่องว่างที่พบจริง
- **Files changed**: `.gitignore`
- **Reason / สิ่งที่พบ**: (1) `supabase/.gitignore` (nested) มี `.env.keys` (dotenvx master key) แต่ root `.gitignore` ไม่มี — ถ้าใช้ dotenvx ที่ root จะหลุด (2) ไม่มี catch-all `*.log` มีแค่ pattern เฉพาะของ npm/yarn/pnpm (3) ไม่มี `Thumbs.db` คู่กับ `.DS_Store` (4) โปรเจกต์นี้ใช้ Claude Code หนัก (`.claude/launch.json`, `.claude/skills/` ถูก track ไว้โดยตั้งใจ) แต่ยังไม่กัน `.claude/settings.local.json` ซึ่งเป็น permission ที่ Claude Code สร้างในเครื่องและไม่ควร commit (ยังไม่มีไฟล์นี้ตอนตรวจ แต่เป็น gap ที่จะเกิดแน่ถ้าไม่กันไว้ก่อน) · ตรวจแล้วว่าไฟล์ที่ถูก track ทั้งหมด (`git ls-files`) ไม่มี secret/artifact หลุดอยู่ก่อน และ `git status --ignored` ครอบคลุมของที่มีอยู่จริงบนดิสก์ครบแล้ว (`.next/`, `node_modules/`, `.gitnexus/`, `qa-screenshots/`, `storybook-static/`, `test-results/`, `supabase/.temp|.branches`, `tsconfig.tsbuildinfo`, `next-env.d.ts`, `.env.local`, `.DS_Store`)
- **Result**: เพิ่ม 4 บรรทัด — `.env.keys`, `*.log`, `Thumbs.db`, `.claude/settings.local.json` (มีคอมเมนต์กำกับว่าไฟล์อื่นใน `.claude/` ยังถูก track ตามเดิม) · ยืนยันด้วย `git status --porcelain --ignored` ว่า rule ใหม่ไม่ชนไฟล์ที่ track อยู่แล้ว (no false positive)

## 2026-09-06 (รอบ 17 — Uploads: ปรับรูปภาพให้ตรง spec §6A ก่อน CP1 — flag ปิด · bucket private · task-only) — branch `feat/poc`

- **Task**: ฟีเจอร์รูปจากรอบ 14 ไม่อยู่ใน POC scope → เจ้าของโปรเจกต์วิเคราะห์/ออกแบบใหม่ใน Design §6A + Scope §5.1/§11 แล้วให้ปรับของเดิมให้ตรง spec: (1) feature flag ซ่อน UI ทั้งหมด + ปิดที่ server action (2) Storage RLS: bucket private + signed URL อายุ ≥1 ชม. ลงชื่อด้วย client ของ user (3) รูปผูก task เท่านั้น — ถอด cover/gallery ของ goal, dashboard/ปฏิทินเหลือ icon + จำนวน (4) ยืนยัน FAB มือถือยังอยู่ (5) บันทึกงาน MVP เป็นงานค้าง · คำตัดสินเพิ่ม: flag ชื่อ `NEXT_PUBLIC_FLAG_UPLOADS` ครอบ avatar ด้วย (PDPA), path `{user_id}/{task_id}/{uuid}`, drop `goal_photos`, ปัด exp ชั่วโมงเต็ม, E2E ห้าม skip เงียบใน CI, ห้ามใช้ service_role ลงชื่อ
- **Files changed**: `src/lib/flags.ts` (ใหม่ — `UPLOADS_ENABLED`), `src/lib/env.ts` (flag ใน clientSchema แบบ optional enum "0"/"1"), `.env.example`, `netlify.toml` (`NEXT_PUBLIC_FLAG_UPLOADS = "0"` ทุก context: production/deploy-preview/branch-deploy/dev), `.github/workflows/ci.yml` (job หลัก flag 0 + job ใหม่ `e2e-uploads`: Supabase local จาก devDependency + Playwright `photos.spec.ts` ด้วย flag 1), `supabase/migrations/20260906180000_uploads_private.sql` (ใหม่ — bucket `public=false`, drop policy public read, select เฉพาะโฟลเดอร์ตัวเอง, update มี `with check`, drop `goal_photos` + trigger function + `goals.cover_path`), `src/types/database.ts` (regen), `src/lib/supabase/storage.ts` (ถอด `photoPublicUrl`; เพิ่ม `photoPathFor`/`isTaskPhotoPath`/`isAvatarPath`/`signedUrlWindow`; `PhotoKind` เหลือ taskPhoto/avatar) + `storage.test.ts` (ใหม่), `src/core/photos/{schema,actions,queries}.ts` (kind เหลือ 2; action ปฏิเสธ `featureDisabled` เมื่อ flag ปิด และตรวจ path ตาม task id; `signPhotoUrls` + cache ต่อ instance คีย์ (user, ชั่วโมง, path) + `withSignedPhotoUrls`), `src/core/tasks/{schema,queries}.ts` (`TaskPhoto.url`; ลงชื่อ batch เดียวต่อ query), `src/core/goals/queries.ts` (ถอด `photos`), `src/core/profile/queries.ts` (`Me.avatarUrl` signed), `src/lib/format.ts` (+ `avatarLetter` — ตัวอักษรแรกของชื่อ ไม่ใช่ initials) + `format.test.ts`, `src/hooks/use-photo-upload.ts` (path ใหม่ + กัน flag), `src/components/domain/TaskPhotos.tsx` (`TaskAttachmentCount` icon+จำนวน · `TaskThumbs` ซ้อนกัน 40px สูงสุด 3 · tile เทา image-off เมื่อ url null), `src/components/domain/TaskRow.tsx` (prop `attachments: none|icon|stack`) + `TaskRow.stories.tsx` (+3 state), `src/components/domain/TaskList.tsx` (ส่ง mode ตาม flag; strip ใต้ flag), `src/components/domain/TaskForm.tsx` (picker ใต้ flag), `src/components/domain/GoalPhotos.tsx` → `AvatarSlot.tsx` (git mv; เหลือ `AvatarSlot` + `LetterAvatar` brand-100/brand-800; flag ปิด = ตัวอักษรอย่างเดียว), `src/components/layout/{TopBar,UserMenu,AppShell}.tsx` (รับ `avatarUrl` signed; fallback เป็น LetterAvatar), `src/app/(app)/settings/page.tsx`, `src/app/(app)/goals/[id]/page.tsx` (ถอด GoalCover/GoalGallery; TaskList `attachments="stack"`), stories GoalCard/GoalCascadeTree/GoalProgressPanel (ลบ fixture `cover_path`), `src/messages/th.json` (ลบ photos.cover/coverPlaceholder/gallery/galleryHint; + photos.unavailable, errors.featureDisabled), `e2e/photos.spec.ts` (เขียนใหม่: flag เปิด = flow task+avatar และตรวจว่า URL เป็น `/object/sign/`, dashboard ไม่มี `<img>`; flag ปิด = ไม่มี UI อัปโหลด; throw ถ้าอยู่ใน CI แล้ว flag ไม่ใช่ 1), `playwright.config.ts` (รองรับ `PORT`), `README.md`, `docs/kemtit-ui-design-system.md` (§6A.2 แถว avatar, §6A.6 ชื่อ flag)
- **Reason / การตัดสินใจ**: (1) ทดสอบเชิงประจักษ์กับ Storage API local: token ของ signed URL มี `iat` → ลงชื่อ 2 ครั้งด้วย `expiresIn` เท่ากันได้ token ต่างกัน การปัด exp อย่างเดียวจึงไม่ทำให้ URL คงที่ → ทำตามเจตนา (browser cache/egress) ด้วย cache ฝั่ง server ต่อ instance คีย์ `${userId}|${hour}|${path}` ลงชื่อครั้งเดียวต่อชั่วโมงเต็ม exp = ต้นชั่วโมงถัดไป + 1 ชม. (อายุจริง 1–2 ชม. — ไม่ปัดลงตรง ๆ เพราะ URL ที่ออก 14:59 จะตายใน 1 นาที) · key มี user id เสมอ → user B ไม่มีทางได้ URL ของ A จาก cache แม้ query layer จะ bug และ paths นอกโฟลเดอร์ตัวเองไม่ถูกส่งไปลงชื่อเลย (2) ลงชื่อด้วย `createServerSupabase()` (cookie session ของ user) → RLS select ใหม่คือตัวตัดสิน ไม่ใช้ service_role (3) drop `goals.cover_path` ด้วยเหตุผลเดียวกับ `goal_photos` (schema ตายทำให้สับสนตอน MVP, ยังไม่มีข้อมูล production) — เจ้าของโปรเจกต์ระบุแค่ตาราง จึงขอให้ตรวจ/veto ได้ (4) `avatarLetter` ต้องอยู่ใน `lib/format.ts` (ไม่มี "use client") เพราะหน้าตั้งค่าเป็น server component เรียกใช้ — ตอนแรกวางใน AvatarSlot แล้วพัง runtime ("Attempted to call avatarLetter() from the server") จับได้จาก E2E (5) `e2e/photos.spec.ts` ของ avatar เดิม `setInputFiles` ทันทีหลัง `goto` แพ้ race กับ hydration ใน dev (handler ยังไม่ผูก อัปโหลดไม่เกิด ไม่มี error) → เปลี่ยนเป็นคลิกช่อง avatar แล้วรอ `filechooser` แบบ `toPass` retry (6) Next 16 ไม่ให้เปิด `next dev` 2 ตัวในโฟลเดอร์เดียว — dev server ที่ค้างที่ :3000 เป็น process กำพร้า (parent = launchd ตั้งแต่ 08:59) จึงหยุดแล้วให้ Playwright เปิดใหม่ที่ `PORT=3100` (7) gitnexus impact: `photoPublicUrl`/`usePhotoUpload`/`TaskThumbs` = CRITICAL, `attachPhoto` = HIGH — caller ทั้งหมดอยู่ในชุดไฟล์ที่แก้ ครอบด้วย typecheck + E2E
- **Result**: typecheck ผ่าน · lint 0 error (warning form.watch เดิม 3 จุด) · unit 67 (เดิม 63 + storage 4 + avatarLetter 1 รวมใน format) · Storybook build ผ่าน · `pnpm build` (webpack, flag 0) ผ่าน · E2E flag เปิด (mobile, PORT=3100): แนบรูปที่งาน + avatar ผ่าน 2/2 — ยืนยัน `<img src>` เป็น `/storage/v1/object/sign/…`, dashboard/TodayTasks ไม่มี `<img>` มีแต่ icon "1 รูป", goal detail มี thumbnail, ลบจาก lightbox ได้ · E2E flag ปิด mobile+desktop 16/16 (photos flag-off 2/2 + regression 7 spec × 2) · FAB มือถือ: `Fab.tsx` render ใน AppShell (`lg:hidden`, ซ่อนเฉพาะ /settings) และ goals.spec มือถือสร้างเป้าผ่าน FAB ผ่าน — เมนู "เพิ่ม" กลาง top bar desktop ถูกถอดตั้งแต่รอบ 14 ไม่กระทบ · **ไม่ทำในรอบนี้ (ตามคำสั่ง)**: งาน MVP ด้านล่าง · **ยังไม่ทดสอบ**: job `e2e-uploads` บน GitHub Actions จริง (เขียนจากคำสั่งที่รันผ่านในเครื่อง: `supabase start/status -o env`, Playwright) — ตรวจผลตอน push ครั้งแรก · ไฟล์เก่าใน bucket local ที่ path `<uid>/goalCover|goalPhoto|taskPhoto/…` ยังค้าง (ไม่มีแถวอ้างถึง ไม่กระทบ) · เจ้าของโปรเจกต์ต้องเพิ่ม `NEXT_PUBLIC_FLAG_UPLOADS=0` ใน `.env.local` เอง (ไม่ตั้ง = ปิดอยู่แล้ว) และ dev server ที่ :3000 ถูกหยุด — เปิดใหม่ด้วย `pnpm dev`

### งานค้าง MVP — Task Attachments (Design §6A · ห้ามทำก่อน CP1 จบ)

- ลิมิต **Free 1 / Pro 5 ต่อ task** ตรวจฝั่ง server จาก `user_profiles.subscription_tier` · tile ถึงลิมิตแสดง "Pro ได้ 5 รูป" ก่อนกด ไม่ใช่ error หลังกด (ปัจจุบัน `TASK_PHOTO_LIMIT = 5` เท่ากันทุกคน)
- ลบไฟล์ใน bucket ผ่าน `domain_events` type **`task.deleted`** + listener ใน `shared-services/` ตามโครงเดิม (`deleteTask` ต้อง emit event; listener ลบทั้งโฟลเดอร์ `<user_id>/<task_id>/`) — ไม่สร้าง cron job แยก
- ย่อรูปฝั่ง client เป็น **WebP กว้างสุด 1600px ≤2MB** ก่อน upload (ปัจจุบัน JPEG 0.85) · รองรับ **HEIC** จาก iPhone (แปลงฝั่ง client) · ตรวจ MIME ฝั่ง server จาก **magic bytes** ไม่ใช่นามสกุล · ปรับ `file_size_limit` ของ bucket จาก 10MB เป็น 2MB และข้อความ `photos.attachHint`/`errors.photoTooLarge`
- Action sheet **"ถ่ายรูป" / "เลือกจากคลัง"** (ถ่ายรูปอยู่บนสุด) ใช้ `capture="environment"`
- **AttachmentGrid 5 state** ตาม §6A.4 (ยังไม่มีรูป / กำลังอัปโหลด thumbnail จาง + ProgressRing ธรรมดา ห้าม compass dial / ล้มเหลว ขอบ danger + ปุ่ม "ลองใหม่" / ถึงลิมิต Free / โหลดไม่ได้) · **Lightbox** ปัดซ้ายขวา + ลบพร้อม **undo toast 5 วิ** · grid 3 คอลัมน์ 1:1 radius 14 ใน task sheet
- อัปโหลดต้อง**ไม่บล็อกการบันทึก task** (ตอนนี้ฟอร์มรอ upload ทีละรูปก่อน toast/ปิด) · tile ล้มเหลวต้องมีปุ่ม "ลองใหม่" ไม่หายเฉย ๆ
- Alt text optional ช่องเดียว (§6A.2) — ยังไม่มีคอลัมน์
- วัด **Lighthouse ใหม่หลังเปิด flag** · รูปนอก viewport ต้อง **lazy-load** (thumbnail ใช้ `loading="lazy"` แล้ว แต่ต้องวัดหน้าจริง)
- เปิด **avatar** พร้อมกัน (flag เดียวกัน) · ทบทวน signed URL cache เมื่อ deploy หลาย instance (cache ต่อ instance — ถ้า egress ยังสูงพิจารณา Next data cache คีย์ (user, ชั่วโมง, path))
- กฎเดิมยังบังคับ: semantic token เท่านั้น · Zod ก่อนแตะ DB · ห้าม hardcode ข้อความ · ทุก component ผ่าน DoD §16

## 2026-09-07 (รอบ 19 — M10a: ชั้นข้อมูล "บันทึกยอด" (goal_entries) ตาม Claude Design turn 6/7) — branch `feat/poc`

- **Task**: นำเข้าดีไซน์รอบใหม่จากโปรเจกต์ Claude Design (`Kemtit.dc.html` turn 6 = D-04 Dashboard v2 / D-07 Goal Detail v2 / D-06 Goals v2 · turn 7 = D-04 Dashboard v3 แนว admin + D-10 ตารางบันทึกยอด) แล้วลงมือ **phase 1 = ชั้นข้อมูลอย่างเดียว** ตามแผน `docs/plans/2026-09-06-design-turn6-7-sales-log.md` (UI อยู่ phase 2/3)
- **Files changed**: `docs/plans/2026-09-06-design-turn6-7-sales-log.md` (ใหม่ — แผน 3 phase ฉบับเต็ม), `supabase/migrations/20260907090000_goal_entries.sql` (ใหม่), `src/types/database.ts` (regen), `src/core/entries/{schema,actions,queries}.ts` (ใหม่), `src/core/domain/entries.ts` + `entries.test.ts` (ใหม่ — pure function + 21 เทสต์), `src/core/goals/completion.ts` (ใหม่ — `markMetricCompletedIfReached` ใช้ร่วมกัน), `src/core/goals/actions.ts` (`updateCurrentValue` เปลี่ยนเป็นเขียน adjustment entry), `src/core/goals/queries.ts` · `src/core/tasks/queries.ts` · `src/core/profile/queries.ts` (ห่อด้วย `cache()` ของ React), `src/core/events/types.ts` (`entry.logged`), `src/components/widgets/GoalProgressPanel.tsx` (ใช้ `paceDelta` แทนสูตร inline), `src/messages/th.json` (`errors.tooLarge` / `goalArchived` / `entryOutsidePeriod`)
- **Reason / การตัดสินใจ**: (1) turn 6/7 ไม่ใช่แค่ restyle — มี domain ใหม่ "บันทึกยอด" (รายการยอดรายวันผูก metric goal มี วันที่/รายการ/ช่องทาง/จำนวน/สถานะ) จึงต้องมี schema ก่อน UI (2) **`goals.current_value` กลายเป็นค่า derived** = `greatest(0, sum(goal_entries.amount))` ผ่าน trigger → `listGoalsWithProgress` (impact = CRITICAL, 11 จุด) กับ `buildProgressIndex`/pace/widget เดิม **ไม่ต้องแก้เลยสักบรรทัด** (3) ฟอร์ม "อัปเดตยอด" เดิมยังอยู่ทุกป้าย (e2e/QA ใช้อยู่) แต่ข้างในแปลงเป็น adjustment entry (ส่วนต่าง) เพื่อให้ประวัติรวมกันได้ตลอด — delta คิดจาก **ผลรวมจริงใน `goal_entries` ไม่ใช่ `goals.current_value`** เพราะ trigger clamp ที่ 0 (พิสูจน์บน DB จริงแล้วว่าสองค่าต่างกันได้: current_value = 0 ขณะผลรวมจริง = −200) (4) บังคับ `entry_date` อยู่ในช่วงของเป้า (`entryOutsidePeriod`) เพราะ trigger รวม entry ทุกใบ แต่กราฟสะสม (phase 2) นับเฉพาะใบในช่วง — ถ้าไม่บังคับ จุดสุดท้ายของกราฟกับตัวเลขบนหน้าปัดจะไม่ตรงกัน · ฟอร์ม "อัปเดตยอด" ไม่ปฏิเสธแต่หนีบวันเข้าขอบช่วงแทน (path เดิมต้องไม่พังกับค่าที่เคยบันทึกได้) (5) status ของรายการ (วันนี้/ยืนยันแล้ว) **คำนวณ ไม่เก็บใน DB** (6) ยังไม่บังคับลิมิต Free/Pro (POC ไม่มี billing และ `e2e/goals.spec.ts` สร้างเป้าที่ 2 ของ user free อยู่)
- **Result**: typecheck ผ่าน · lint 0 error (เหลือ warning `form.watch` เดิม 3 จุด) · unit **89 tests ผ่าน** (เดิม 68 + 21 ใหม่) · migration apply บน local ครบ 5/5
  - **ตรวจ trigger กับ DB จริง** (ใน transaction ที่ rollback ทั้งหมด ไม่มีข้อมูลทดสอบตกค้าง): insert 2 ใบ → 10,400 ✓ · update 5,600→1,000 → 5,800 ✓ · delete → 4,800 ✓ · ยอดสุทธิ −200 → `current_value` = 0 (clamp) ✓ · `amount = 0` ถูกปฏิเสธ ✓ · ผูก execution goal ถูกปฏิเสธ ✓ · ผูก goal ของ user อื่นถูกปฏิเสธ ✓ · ย้าย `goal_id` ตอนแก้ถูกปฏิเสธ ✓ · ลบ goal แล้ว entry หายตาม cascade ✓
  - backfill: metric goal เดิม 36 แถวกลายเป็น entry "ยอดเริ่มต้น" และ `current_value` ตรงกับผลรวมทุกแถว (query ตรวจความไม่ตรง = 0) · RLS เปิด · `anon` ไม่มีสิทธิ์ใด ๆ
- **ยังไม่ทำ (phase 2/3 ตามแผน)**: desktop shell v3 (sidebar 2 หมวด + ค้นหา + กระดิ่ง + avatar chip), dashboard v3 (KPI 4 ใบ + กราฟ + การ์ดเข็มทิศที่บันทึกยอดได้ + ตารางล่าสุด), goals v2 / goal detail v2 (กราฟ + ประวัติแก้ได้), หน้า `/entries` + ส่งออก CSV, `/tasks`, `/search`, E2E ของ entries · จุด Pro ทั้งหมดเป็นป้าย/toast "เร็ว ๆ นี้" ไม่บังคับลิมิตจริง

## 2026-09-07 (รอบ 20 — M10b-1: desktop shell v3 ตาม Claude Design turn 7) — branch `feat/poc`

- **Task**: ทำ **phase 2a** ของแผน `docs/plans/2026-09-06-design-turn6-7-sales-log.md` §3.1–§3.4 (ส่วนย่อยของ phase 2 — เฉพาะ "desktop shell" ไม่รวม dashboard v3 / goals v2 / goal detail v2 / หน้าใหม่): โครง `useIsDesktop`/`ResponsiveSwitch` (§1.10), เมนู sidebar 2 หมวด (`nav-items.ts` v3), `Sidebar` v3 + `ProCard`, `AppShell`/`ShellFrame` คำนวณและส่งต่อ "shell data" (งานค้าง/เชื่อม LINE/แพ็กเกจ), `TopBar` v3 (ช่องค้นหา + `NotificationsMenu` + `UserMenu` variant "chip"), `PageHeader` เพิ่ม prop `breadcrumb` (additive), และ i18n keys ที่ชิ้นส่วนเหล่านี้ต้องใช้
- **Files changed**: `src/hooks/use-is-mobile.ts` (+`useIsDesktop`), `src/components/layout/ResponsiveSwitch.tsx` (ใหม่ — ยังไม่ผูกกับหน้าไหน), `src/components/layout/nav-items.ts` (`NavItem`/`NAV_SECTIONS`/`MOBILE_NAV_ITEMS`; `NAV_ITEMS` เหลือเป็น alias), `src/components/layout/BottomNav.tsx` (import `MOBILE_NAV_ITEMS` แทน `NAV_ITEMS`), `src/components/layout/Sidebar.tsx` (v3 เต็ม — โลโก้เข็มทิศ + หมวด "หลัก"/"เพิ่มเติม" + badge + แถวโปรไฟล์ท้าย), `src/components/layout/ProCard.tsx` (ใหม่), `src/components/layout/AppShell.tsx` (เป็น async — คำนวณ `openTasks`/`overdue`/`lineLinked`/`tier` จาก `getDayPlan`+`me.profile`), `src/components/layout/ShellFrame.tsx` (รับ `shell`/`profile` ส่งต่อ `Sidebar`), `src/components/layout/TopBar.tsx` (v3 desktop cluster; มือถือ classNames เดิมทุกจุด), `src/components/layout/NotificationsMenu.tsx` (ใหม่), `src/components/layout/UserMenu.tsx` (+prop `variant`/`persona`; variant เดิม byte-identical), `src/components/layout/PageHeader.tsx` (+prop `breadcrumb`, เป็น async server component ด้วย `getTranslations` เพื่อดึงคำว่า "Kemtit"; แก้ `lg:pr-80`→`lg:pr-[520px] xl:pr-[600px]` + เพิ่ม `truncate` บน h1 แบบ global ตามที่แผนสั่ง), `src/messages/th.json` (`nav.{entries,tasks,reports,sectionPrimary,sectionMore,proSoon,search,searchPlaceholder,notifications,addEntry}`, namespace ใหม่ `pro.*`, `notifications.*`)
- **Reason / การตัดสินใจ**: (1) `useIsDesktop()`/`ResponsiveSwitch` สร้างไว้เป็นโครงเฉย ๆ ยังไม่ผูกกับหน้าไหน (dashboard v3 = phase 2b) — เลี่ยงปัญหา "สอง tree ใน DOM" ที่ repo นี้เจอมาแล้ว 2 ครั้ง (รอบ 13/14) แต่พร้อมใช้ทันทีที่ phase 2b เริ่ม (2) `TopBar` ให้ mobile cluster (persona pill + `UserMenu` avatar) กับ desktop cluster (ค้นหา + กระดิ่ง + `UserMenu` chip) อยู่ใน DOM พร้อมกันเสมอ สลับด้วย CSS `lg:hidden`/`hidden lg:flex` เท่านั้น (ไม่ใช้ `useIsDesktop` แบบ dashboard) — ปรึกษา advisor แล้วยืนยันว่าอันตรายของรอบ 13/14 อยู่ที่ `getByText` ที่ไม่กรอง `display:none` เท่านั้น ส่วน `getByRole` กรอง accessibility tree ให้อยู่แล้ว (พิสูจน์จาก `Sidebar`/`BottomNav` ที่มี `<nav aria-label="เมนูหลัก">` ซ้ำกันแบบนี้อยู่ก่อนแล้วและ `localhost-qa.spec.ts` ที่ query ด้วย `getByRole` แบบไม่ scope ก็ยังผ่าน) — ทำแบบ CSS จึงไม่ต้องเปลี่ยน `TopBar` เป็น client component และไม่กระทบ native `<form action="/search">` (3) `PageHeader` ต้องเป็น `async function` + `getTranslations()` (ไม่ใช่ `useTranslations`) เพื่อดึงคำว่า "Kemtit" ผ่าน th.json ตามกฎ "ทุกข้อความผ่าน next-intl" โดยไม่ต้องเติม `"use client"` ให้ 5 หน้าที่ใช้มันอยู่ (4) `NAV_ITEMS` (`risk: UNKNOWN` จาก gitnexus — 0 caller ที่ graph มองเห็น) ยืนยันด้วย text-search ว่ามีแค่ `Sidebar`/`BottomNav` — ทั้งคู่แก้ไปใช้ `NAV_SECTIONS`/`MOBILE_NAV_ITEMS` แล้ว จึงเหลือ `NAV_ITEMS` เป็น alias ไว้เฉย ๆ กันของอื่นที่มองไม่เห็นพัง (5) `AppShell` เปลี่ยนจาก sync เป็น async เพื่อเรียก `getDayPlan(today)` ที่ห่อ `cache()` ไว้แล้ว (phase 1) — ทุกหน้าที่ login แล้วจะยิง query นี้เพิ่ม (ไม่ใช่แค่ dashboard) แต่ dedupe เหลือ query เดียวต่อ request; หน้าไหนเรียกซ้ำ (เช่น dashboard) ก็ไม่มีต้นทุนเพิ่ม (6) ทดสอบจริงบน dev server พบ `Sidebar` ล้นที่ viewport 1280×720 (ค่า default ของ Playwright Desktop Chrome): `nav.scrollHeight` 455px > `nav.clientHeight` 429px ทำให้ "ตั้งค่า" โผล่ไม่เต็มถ้าไม่เลื่อน — ลดระยะห่างที่ไม่ใช่ตัวเลขล็อกตามแผน (section label `pt-4 pb-1.5`→`pt-3 pb-1`, `ProCard` `p-3.5 gap-2`→`p-3 gap-1.5`, แถวโปรไฟล์ `pt-3.5`→`pt-2.5`) จนเท่ากันพอดี (7) impact ก่อนแก้: `PageHeader` HIGH (5 หน้า — ยืนยันว่า byte-identical ทุกจุดยกเว้น padding-right กับ truncate ที่แผนสั่งให้เป็น global), `useIsMobile` CRITICAL (19 จุดพึ่งพา) — ไม่แตะโค้ดเดิมเลย เพิ่มแค่ `useIsDesktop` ใหม่ต่อท้าย, `getMe`/`getDayPlan` CRITICAL/HIGH — เรียกใช้เฉย ๆ ไม่แก้ signature, `Sidebar`/`TopBar`/`AppShell`/`ShellFrame`/`UserMenu`/`BottomNav` ทั้งหมด LOW
- **Result**: `pnpm typecheck` ผ่าน · `pnpm lint` 0 error (เหลือ warning `form.watch` เดิม 3 จุด) · `pnpm test` **89/89 ผ่าน** (ไม่เพิ่มเทสต์ใหม่ในรอบนี้) · `pnpm build-storybook` ผ่าน · `pnpm e2e e2e/dashboard.spec.ts e2e/goals.spec.ts e2e/tasks.spec.ts e2e/calendar.spec.ts` **10/10 ผ่านทั้ง 2 projects** (mobile-chrome + desktop-chrome) · `detect_changes({scope:"all"})` ยืนยันว่ามีแค่ไฟล์/symbol ที่ตั้งใจแก้เท่านั้น (`AppShell`/`BottomNav`/`PageHeader`/`ShellFrame`/`Sidebar`/`TopBar`/`UserMenu`/`nav-items.ts`) ไม่แตะ symbol ธุรกิจ CRITICAL ใด ๆ · ตรวจด้วยตาผ่าน dev server จริง (ใช้ session ที่ login ค้างอยู่แล้วในเบราว์เซอร์ ไม่ได้สร้าง auth ใหม่) ที่ 1440×900, 1280×720, 375×812: sidebar 2 หมวด + badge "งาน 2" + `ProCard` "แพ็กเกจ Free" + พับ/ขยายเมนูถูกต้อง (aria-label `nav.collapse`/`nav.expand` เดิม), `TopBar` desktop มีช่องค้นหา + กระดิ่ง (แสดง "งานค้าง 2 รายการ" + "ยังไม่ได้เชื่อม LINE") + user chip ("ui-check-…@kemtit.test · พ่อค้าแม่ค้าออนไลน์"), มือถือ (375px) หน้าตาเดิมทุกจุด (persona pill + avatar + bottom nav 4 แท็บ + FAB) ไม่มี console error
- **ยังไม่ทำ**: `ResponsiveSwitch` ยังไม่ถูกเรียกใช้จากหน้าไหน (dashboard v3 desktop content = phase 2b ตามแผน) · `/search` `/entries` `/tasks` ยังไม่มีหน้าจริง — ลิงก์ sidebar ชี้ไปแล้วตั้งใจให้ 404 ก่อน (phase 3) · `breadcrumb` prop ของ `PageHeader` ยังไม่มีหน้าไหนส่งมาใช้จริง (รอ dashboard/page.tsx ผูกใน phase 2b) จึงยังไม่เห็นบรรทัด "Kemtit › …" ของจริง · **ความเสี่ยงที่ตั้งใจปล่อยไว้**: `e2e/qa/localhost-qa.spec.ts` บรรทัด `expect(page.getByText("พ่อค้าแม่ค้าออนไลน์")).toBeVisible()` (ขั้น `desktop-shell-sidebar-topbar`, ไม่รันใน CI) จะ resolve เจอ 2 element (persona pill มือถือ + user chip desktop มีข้อความเดียวกัน ซ่อนด้วย CSS ไม่ใช่ conditional render) — ต้อง scope locator นี้ใหม่ (เช่น `.first()` หรือผูกกับปุ่ม chip) เมื่อแตะไฟล์นี้ครั้งหน้า — ไม่อยู่ใน 4 spec ที่โจทย์รอบนี้สั่งให้รัน จึงยังไม่แก้ · ไม่ได้ล็อกอินใหม่ผ่าน OTP/Mailpit — ใช้ session ที่ล็อกอินค้างอยู่แทน

## 2026-09-07 (รอบ 21 — M10b-2: dashboard v3 + component ชุด "บันทึกยอด" ตาม Claude Design turn 6/7) — branch `feat/poc`

- **Task**: ต่อจาก M10b-1 (shell v3) — ประกอบแดชบอร์ด desktop v3 ตาม turn 7 "7a" และสร้าง component ชุดบันทึกยอดที่หน้า `/entries` (phase 3) จะใช้ซ้ำ
- **Files changed**: `src/components/domain/SalesChart.tsx` + `SalesChart.stories.tsx` (ใหม่), `QuickEntryForm.tsx` · `QuickTaskInput.tsx` · `EntryForm.tsx` · `EntriesTable.tsx` + `EntriesTable.stories.tsx` (ใหม่ทั้งหมด), `src/components/domain/StatTile.tsx` (ทรง KPI ใหม่เมื่อส่ง `icon` — ทรงเดิมไม่เปลี่ยน), `src/components/ui/segmented-nav.tsx` (รองรับตัวเลือก disabled + ไอคอนล็อก), `src/lib/format.ts` (`monthShort`), `src/core/tasks/queries.ts` (`getWeekTaskStats`), `src/app/(app)/dashboard/desktop-dashboard.tsx` (ใหม่), `src/app/(app)/dashboard/page.tsx`, `src/components/widgets/TodayTasksWidget.tsx` (prop `quickAdd`), `src/app/(app)/layout.tsx` · `src/components/layout/QuickAddHost.tsx` · `QuickAddMenu.tsx` (รองรับ `?new=entry`), `src/messages/th.json` (namespace `entries.*` ทั้งชุด + `widgets.todayTasks.quickPlaceholder`), `e2e/dashboard.spec.ts`
- **Reason / การตัดสินใจ**: (1) มือถือ ↔ desktop สลับด้วย `ResponsiveSwitch` ไม่ใช่ `hidden lg:block` — repo เจอ Playwright strict mode ล้มจากการซ่อนด้วย CSS มาแล้ว 2 ครั้ง (รอบ 13/14) ผลคือ **ไม่ต้องแก้ locator เดิมสักตัว** แลกกับการที่ server เรนเดอร์ทั้งสองต้นไม้ (วัดจริงบน dev + Supabase local: `GET /dashboard` ~700 มิลลิวินาที รับได้ในระดับ POC) (2) SVG ของกราฟใช้ token ผ่าน class ล้วนเหมือน `CompassDial` ไม่มี hex ดิบ (eslint บังคับ) (3) ปุ่มไอคอนในตารางคงขนาดตา 32px ตามดีไซน์ แต่ขยายพื้นที่กดเป็น 44px ด้วย pseudo-element แทนการขยายกล่อง — ความหนาแน่นของแถวไม่เปลี่ยน (4) ชิป +500/+1,000/+2,000 **บวกทับ**ค่าที่พิมพ์อยู่ ตาม logic ต้นแบบ (5) ฟอร์มบันทึกยอดซ่อน Select เมื่อมีเป้าให้เลือกตัวเดียว แล้วบอกชื่อเป้าเป็นข้อความแทน (6) `EntryForm` เลี่ยง `form.watch` (React Compiler memo ไม่ได้) โดยเก็บเป้าที่เลือกไว้ใน state คู่ขนาน — จำนวน warning ของ repo จึงยังเท่าเดิม (7) การ์ด "งานวันนี้" บน desktop ใช้ `TodayTasksWidget` ตัวเดิม + prop `quickAdd` เพื่อคง region name และลิงก์ `aria-label="เพิ่มงาน"` ที่ QA step พึ่งอยู่ (toolbar เดิมถูกถอดไปแล้วใน v3)
- **Result**: typecheck ผ่าน · lint **0 error** (warning `form.watch` เดิม 3 จุดเท่าเดิม) · unit **89** ผ่าน · Storybook build ผ่าน · E2E 4 spec × 2 project (dashboard/goals/tasks/calendar) **ผ่าน 10/10**
  - **หมายเหตุเรื่องการวัดผล**: รอบแรก e2e ล้ม 10/10 และรอบต่อมาล้ม 2/10 โดยใช้เวลา 32 นาที — ตรวจแล้วไม่ใช่โค้ด แต่เป็น **เครื่องหลับกลางรัน** (รันแยก `calendar.spec` ใหม่: เทสต์ใช้เวลาจริง 8.6 วิ + 10.2 วิ แต่ wall clock 16.6 นาที) และ log ของ dev server ยืนยันว่า `GET /dashboard` ตอบ 200 ใน ~700 มิลลิวินาทีตลอด ไม่มีค้าง
- **ยังไม่ทำ (phase 2c/3 ตามแผน)**: GoalCard v2 + goal detail v2 (กราฟ + ประวัติแก้ได้ + ปุ่มแชร์), หน้า `/entries` + ตัวกรอง + pagination + ส่งออก CSV, หน้า `/tasks`, `/search`, `e2e/entries.spec.ts`, อัปเดต QA spec สำหรับ dashboard v3 · ลิงก์ sidebar ไป `/entries` `/tasks` `/search` ยังชี้ไปหน้าที่ยังไม่มี (ตั้งใจ — สร้างใน phase 3)
